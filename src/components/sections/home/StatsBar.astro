---
const stats = [
  '50+ Homes Built',
  '5-Day Quote Turnaround',
  'Greater Houston Coverage',
  'Owner-Led & Insured',
];
---

<section class="bg-navy py-12 px-4">
  <div class="container-main">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
      {stats.map((stat) => (
        <div>
          <span class="text-gold font-display text-3xl md:text-4xl tracking-[0.03em] leading-tight block">
            {stat}
          </span>
        </div>
      ))}
    </div>
  </div>
</section>
